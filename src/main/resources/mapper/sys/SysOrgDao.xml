<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wp.modules.sys.dao.SysOrgDao">
	
	<select id="countOrgChildren" resultType="int">
		SELECT
			COUNT(*)
		FROM
			sys_org
		WHERE
			deleted = 0
			AND parent_id = #{value}
	</select>

	<select id="listOrgChildren" resultType="long">
		SELECT
			org_id
		FROM
			sys_org
		WHERE
			deleted = 0
			AND parent_id = #{value}
	</select>

	<select id="getChildren" resultType="com.wp.modules.sys.entity.SysOrgEntity">
		SELECT
			o.org_id,
			o.parent_id,
			(
				SELECT
					p.name
				FROM
					sys_org p
				WHERE
					p.org_id = o.parent_id
			) AS parent_name,
			o.code,
			o.name,
			o.status,
			o.order_num,
			o.create_time,
			o.create_user_id,
			o.update_time,
			o.update_user_id
		FROM
			sys_org o
		WHERE
			o.deleted = 0
			AND o.parent_id = #{parentId}
		ORDER BY
			o.order_num ASC
	</select>
	
	<select id="list" resultType="com.wp.modules.sys.entity.SysOrgEntity">
		SELECT
			o.org_id,
			o.parent_id,
			(
				SELECT
					p.name
				FROM
					sys_org p
				WHERE
					p.org_id = o.parent_id
			) AS parent_name,
			o.code,
			o.name,
			o.status,
			o.order_num,
			o.create_time,
			o.create_user_id,
			o.update_time,
			o.update_user_id
		FROM
			sys_org o
		WHERE
			o.deleted = 0
		ORDER BY
			o.order_num ASC
	</select>
	<insert id="save">
		INSERT INTO sys_org (
			org_id,
			parent_id,
			code,
			`name`,
			order_num,
			status,
			`deleted`,
			create_time,
			create_user_id,
			update_time,
			update_user_id
		)
		VALUES (
			#{orgId},
			#{parentId},
			#{code},
			#{name},
			#{orderNum},
			#{status},
			#{deleted},
			#{createTime},
			#{createUserId},
			#{updateTime},
			#{updateUserId}
		)
	</insert>
	
	<select id="getObjectById" resultType="com.wp.modules.sys.entity.SysOrgEntity">
		SELECT
			o.org_id,
			o.parent_id,
			(
				SELECT
					p.name
				FROM
					sys_org p
				WHERE
					p.org_id = o.parent_id
			) AS parentName,
			o.code,
			o.name,		
			o.status,
			o.deleted,
			o.order_num,
			o.create_time,
			o.create_user_id,
			o.update_time,
			o.update_user_id
		FROM
			sys_org o
		WHERE
			o.org_id = #{value}
	</select>
	
	<update id="update">
		UPDATE sys_org
		<set> 
			<if test="orgId != null">org_id = #{orgId}, </if>
			<if test="code != null">code = #{code}, </if>
			<if test="name != null">name = #{name}, </if>
			<if test="status != null">status = #{status}, </if>			
			<if test="orderNum != null">order_num = #{orderNum}, </if>
			<if test="deleted != null">deleted = #{deleted}, </if>
			<if test="createTime != null">create_time = #{createTime}, </if>
			<if test="createUserId != null">create_user_id = #{createUserId}, </if>
			<if test="updateTime != null">update_time = #{updateTime}, </if>
			<if test="updateUserId != null">update_user_id = #{updateUserId} </if>
		</set>
		where org_id = #{orgId}
	</update>
	<update id="batchDelete">
		UPDATE sys_org
		SET
			deleted = 1
		WHERE
		org_id IN
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</update>
	
	<delete id="batchRemove">
		DELETE
		FROM
			sys_org
		WHERE
			org_id IN
		<foreach item="id" collection="array" open="(" separator="," close=")">
			#{id}
		</foreach>
	</delete>
	
</mapper>